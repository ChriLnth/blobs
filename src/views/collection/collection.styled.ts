import { styled } from "@/stitches"

export const Container = styled('div', {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  flexDirection: 'row',
  width: 420,
  flexWrap: 'wrap',
  gap: 50,
})
